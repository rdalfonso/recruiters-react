import React, { Fragment } from "react";
import styles from "./Header.scss";

const Header = (): React.ReactElement => (
  <Fragment>
    <div className={styles.links}>Header!!</div>
  </Fragment>
);

export default Header;
